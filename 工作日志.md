# 工作日志

## 2026-06-20 — 以实践论/矛盾论为基准重建 CSS

### 背景
- 用户将实践论和矛盾论恢复为原始版本（921行，全内联 CSS），作为样式基准
- 先前从 `_template.html` 提取的共享 CSS 与基准差距较大（Tab圆角、内容卡片样式、黑夜模式强调色等均有偏差）
- 基准 CSS 硬编码 `#8b0000`（哲学红），共享版需改为 `var(--cat-primary)` 以支持六类别色值

### 完成

1. **`css/articles-detail.css` 完全重写**
   - 逐行对照基准 CSS，保留所有选择器、属性值、间距
   - 颜色从硬编码改为 `var(--cat-primary)` / `var(--cat-secondary)` / `var(--cat-gradient)`
   - 基准类名保留：`.guide-block`, `.diff-item`, `.dialogue-block`, `.action-item`, `.chart-block`, `.reading-item`
   - 基准特有样式全量保留：`.wrong`/`.correct`（红绿对照块）、时间轴 `::after` 连线、理论节点四色
   - 黑夜模式强调色统一为 `#7986cb`（靛蓝，与基准一致）
   - 旧类名兼容层（`.reading-tip`, `.difficulty-item`, `.dialogue-item`, `.further-item`）→ 过渡期保留
   - 动态章节导航 `.dynamic-chapter-nav` 融入文件末尾

2. **基准文件添加 CSS 引用**
   - `shi-jian-lun.html`、`mao-dun-lun.html` 添加 `<link rel="stylesheet" href="../css/articles-detail.css">`
   - 内联 CSS 保持不动（冗余但安全）

### 后续待办

- [ ] 逐篇检查 26 篇非基准文章，确认与基准 CSS 兼容
- [ ] 统一文章 HTML 结构（`article-detail` vs `article-container`）
- [ ] 合集功能：文章页面包屑 + 上/下一章导航（`main.js` 已实现，待联调）
- [ ] 长文阅读增强：侧边章节导航（`main.js` 已实现）
- [ ] 类别筛选：`articles.html` 双维度筛选已上线
- [ ] `shi-jian-lun.html` 标题仍显示「矛盾论」— 需手动修正为「实践论」

---

## 2026-06-19 — CSS 统一迁移 & 专栏上线

### 专栏文章
- 新建 `international-column/` 文件夹
- `tieliu-yu-xianfeng.html`：铁流与先锋（中俄早期革命中的党与阶级），九章+附章，约35000字
- `international-column.html`：专栏展示页（精选卡片 + 国家专题 + 预告区）
- 右侧悬停快速导航

### CSS 统一（已废弃，被基准版替换）
- ~~创建 `css/articles-detail.css`，从 `_template.html` 提取 517 行公共样式~~
- ~~28 篇文章批量迁移：删除大块内联 CSS，保留 `:root` 色值块~~
- 问题：模板是空骨架，导致拼图/时间轴/结构分析等专用样式丢失

### 合集功能
- `data/collections.json`：3 个合集（毛选第一卷、毛泽东军事思想、毛泽东哲学方法论）
- `articles.html`：合集卡片区 + 弹窗浏览
- 合集导航（面包屑+前/后章）已嵌入 `main.js`

### 已知问题
- `articles.html` 类别筛选已添加第二排按钮，部分卡片类别映射待微调
- `zhan-lue-wen-ti.html` 章节结构已从 16 个平级折叠修复为 5 章层级
- `fan-du-lin-lun.html` 等文件保留文章特有覆盖（左侧FAB、理论拼图增强等）
