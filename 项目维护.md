# QmlmReader 项目维护文档

> 青年马列毛主义驿站 — 马克思主义理论阅读与学习平台
> 最后更新：2026-07-25（全站 29 篇经典文章规范化完成）

---

## 零、工作日志

> 以下为 2026-06-19/20 原始工作日志的核对汇总。全部事项已于 2026-07-25 规范化工程中完成。

| # | 事项 | 状态 |
|---|------|------|
| 1 | `css/articles-detail.css` 完全重写 | ✅ |
| 2 | 基准文件（实践论/矛盾论）CSS 引用 + 内联保留 | ✅ |
| 3 | 逐篇视觉校对 26 篇非基准文章——2026-07-25 全部完成 | ✅ |
| 4 | 统一文章 HTML 结构（`article-detail` vs `article-container`） | ✅ |
| 5 | 合集功能：面包屑 + 上/下一章导航（`main.js`） | ✅ |
| 6 | 长文阅读增强：侧边章节导航（`main.js`） | ✅ |
| 7 | 类别筛选：articles.html 三维度复选框筛选 | ✅ |
| 8 | `shi-jian-lun.html` 标题修正为「实践论」 | ✅ |
| 9 | 专栏文件夹与文章（`international-column/` 含 3+1） | ✅ |
| 10 | CSS 统一迁移（基准版 `articles-detail.css` 完整替换） | ✅ |
| 11 | 合集 data/collections.json + articles.html | ✅ |
| 12 | 合集导航嵌入 main.js | ✅ |
| 13 | articles.html 类别筛选 | ✅ |
| 14 | `zhan-lue-wen-ti.html` 章节层级修复 | ✅ |
| 15 | `fan-du-lin-lun.html` 特有覆盖保留 | ✅ |

### 当前待办

```
[ ] P0："文艺"增加“趣闻”栏：展示苏联笑话和国际其他有意思的事件；经典语录随日历每天随机展示一则。
[ ] P1：修复26篇经典文章中正文缺失的部分。其中实践论的原文部分错误誊抄为矛盾论。
[ ] P2：搜索索引补全（2 篇未加入）、导航栏统一——所有页面共用一份导航，不再复制粘贴
[ ] P3：国际共运子页面内容补全（时事、纪念）。纪念扩容
[ ] P4：留言板后端接入、实验性工具开发
[ ] P5：导师篇扩容：增加导师传记和人物关系网
[ ] P6：更新公告接入“关于”页

```

---

## 一、文件架构

> 2026-07-23 重构：所有 HTML 收入 `html/`，文章按导师分入 `articles/Mao/` 等子文件夹。

```
Qmlmreader/
├── css/ style.css / articles-detail.css    # 全局样式
├── js/ main.js / search.js / darkmode.js / shutter.js / cursor.js
├── data/ / images/ / assets/ / downloads/  # 资源
├── _tools/                                 # 构建脚本
│
└── html/                                   # 【所有页面】
    ├── index.html                          # 首页
    │
    ├── articles/
    │   ├── articles.html                   # 文章列表（导航入口）
    │   ├── _template.html                  # 文章模板
    │   ├── Mao/       (12篇)               # 毛泽东
    │   ├── Marx/      (7篇)                # 马克思
    │   ├── Engels/    (2篇)                # 恩格斯
    │   ├── Lenin/     (6篇)                # 列宁
    │   └── Stalin/    (2篇)                # 斯大林
    │
    ├── masters/
    │   ├── masters.html                    # 导师总览
    │   ├── marx/marx.html                  # 马克思
    │   ├── engels/engels.html              # 恩格斯
    │   ├── lenin/lenin.html                # 列宁
    │   ├── stalin/stalin.html              # 斯大林
    │   └── mao/mao.html                    # 毛泽东
    │
    ├── toolkit/ toolkit.html / science.html
    ├── gallery/ gallery.html / music.html / videos.html / poetry.html / quotes.html
    │   └── propaganda/ propaganda.html / soviet.html / photos.html
    ├── puzzle/ puzzle.html
    ├── international/
    │   ├── international.html / international-calendar.html
    │   ├── international-current.html / international-memorial.html
    │   ├── international-column.html       # 专栏规划页
    │   └── international-column/           # 3篇专栏（均已完成）
    ├── rectify/ rectify.html + economy/leaders/military/myths/
    ├── about/ about.html / changelog-detail.html / changelog-archive.html
    └── experimental/ experimental.html / message-board.html
│   └── cursor.js                     # 自定义光标 + 光辉（所有页面，移动端自动禁用）
├── data/                             # JSON/JS 数据文件
├── assets/posters/                   # 图片资源（中国宣传画/苏联海报）
├── images/                           # 图标/导师肖像
├── downloads/                        # 下载资源
└── _tools/                           # 构建/批处理脚本

### 关键数字

| 指标 | 数量 |
|------|------|
| 文章 HTML | 29 篇（articles/）+ 3 篇（international-column/）+ 9 篇（rectify/）|
| 页面（HTML） | 72 个（含子页面和文章） |
| 子文件夹（导航入口） | 8 个：articles/ masters/ toolkit/ gallery/ puzzle/ international/ rectify/ about/ |
| CSS 文件 | 2 个 |
| JS 文件 | 5 个（main/search/darkmode/shutter/cursor） |
| 合集定义 | 9 组，含约 80 篇文章条目（约 50% 为 wip） |
| 图片资源 | 约 860+ 张海报 |

---

## 二、视觉风格

### 2.1 设计语言

- **主色调**：中国红 `#c41e3a` / 深红 `#8b0000`，辅以金色 `#ffd700`
- **背景**：浅灰 `#f5f5f5`，文章区暖白
- **字体**：系统无衬线栈（`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto ...`），文章标题用宋体栈（`STSong, SimSun, Noto Serif SC`）
- **卡片**：白底圆角 + 轻微阴影，hover 上浮
- **导航栏**：红色渐变 + sticky 置顶 + 金色下划线激活态
- **文章头部**：渐变底色 + SVG 圆环纹理 + 金色标签

### 2.2 CSS 变量体系

```css
/* style.css — 全局变量 */
:root {
    --primary-color: #c41e3a;
    --primary-dark: #8b0000;
    --secondary-color: #ffd700;
    --text-color / --text-light / --bg-color / --card-bg / --border-color
    --shadow / --shadow-hover
}

/* articles-detail.css — 文章类别色（每篇文章 :root 覆盖） */
:root {
    --cat-primary: #8b0000;      /* 主题色 */
    --cat-secondary: #c41e3a;    /* 辅助色 */
    --cat-gradient: linear-gradient(...);
}
```

### 2.3 黑夜模式

- 通过 `body.dark-mode` 切换，覆盖所有 CSS 变量及组件背景色
- 切换按钮位于导航栏最右侧（🌙），通过 `toggleDarkMode()` 函数 + `localStorage` 记忆
- 文章详情黑夜强调色统一为 `#7986cb`（靛蓝）
- 卡片/搜索/背景均在 `style.css` 和 `articles-detail.css` 有对应深色规则

### 2.4 响应式策略

- 桌面（≥769px）：3 列网格，汉堡菜单隐藏
- 手机（≤768px）：2 列网格，全屏汉堡菜单，Tab 横向滚动，描述文字 2 行省略

### 2.5 首页特效（2026-07-23 新增）

- **开屏动画**："全世界无产者，联合起来！" 全屏居中 → 停留 2.5s → 上移淡出（CSS @keyframes）
- **百叶窗轮播**：16 片垂直叶片 Y 轴翻转切换，8 张卡片自动轮播 5s/张，点击跳转对应文章（Canvas 渲染卡片图像）
- **自定义光标**：镰刀锤头 / 红五星随机，CSS radial-gradient 光晕跟随，hover 放大，移动端自动禁用

---

## 三、网页管理

### 3.1 导航结构

```
全局导航栏（所有页面共享）
├── 首页          index.html
├── 文章          articles.html
├── 导师          masters.html
├── 工具集        toolkit.html
├── 文艺          gallery.html
├── 🧩 理论拼图    puzzle.html          （金色高亮）
├── 🌍 国际共运    international.html
├── 正名          rectify.html
└── 关于          about.html
```

### 3.2 导航实现

- **汉堡菜单**：`main.js` L157-218，全屏遮罩 + ESC 关闭 + resize 自适应
- **导航高亮**：根据 `window.location.pathname` 自动匹配 `active` 类
- **黑夜模式按钮**：独立于导航链接，`onclick="toggleDarkMode()"`
- **返回按钮**：文章详情页顶部 `.back-nav`，含面包屑导航（合集上下文由 `main.js` 动态注入）

### 3.3 搜索系统

`js/search.js` 维护 4 套索引：
1. **文章索引**（`articleIndex`）：29 篇经典著作
2. **文艺索引**（`galleryIndex`）：影视/音乐/宣传画
3. **正名索引**（`rectifyIndex`）：正名文章 + 历史搜索
4. **历史事件索引**（`historyIndex`）：约 100+ 条事件

实时搜索（`<input>` 输入即搜），结果下拉展示，含优先级/年代/分类标签。

### 3.4 合集系统

`data/collections.json` 定义 9 组合集：

| 合集 | 文章数 | ready 率 |
|------|--------|----------|
| 毛选第一卷 | 17 | 4 ready |
| 毛选第二卷 | 14 | 3 ready |
| 毛泽东军事思想 | 4 | 4 ready |
| 毛泽东哲学方法论 | 2 | 2 ready |
| 马克思恩格斯选集 | 12 | 5 ready |
| 列宁选集 | 7 | 4 ready |
| 斯大林著作选 | 6 | 1 ready |
| 政治经济学经典 | 8 | 3 ready |
| 党的建设 | 8 | 2 ready |

合集弹窗（`articles.html`）实现：
- 卡片网格展示合集 → 点击弹出目录
- 目录内已收录文章可点击跳转，筹备中文章灰显
- 文章详情页动态注入面包屑 + 上/下一章导航（`main.js` L376-469）
- 一篇文章属于多个合集时，按钮显示 "+N 个合集"，弹窗可切换

### 3.5 章节导航

- **动态章节导航**：长文章自动检测 `.chapter` 元素，生成右侧侧边导航
- **展开/收起手柄**：左下角"导航"按钮，偏好记忆（sessionStorage）
- **滚动高亮**：随页面滚动自动高亮当前章节

### 3.6 文章筛选

`articles.html` 三维度复选框筛选：
- 优先级（⭐1-5）
- 作者（马克思/恩格斯/列宁/斯大林/毛泽东）
- 类型（哲学基础/政治理论/军事战略/政治经济学/党的建设/思想文化）

---

## 四、长线目标

### 4.1 内容扩充

1. **毛选全集**：当前第一卷仅 4/17 ready，第二卷 3/14 ready。目标逐篇制作 HTML
2. **马恩选集补全**：12 篇计划中仅 5 篇 ready，需补充《法兰西内战》《社会主义从空想到科学》等
3. **列宁/斯大林著作补全**：大量 wip 状态
4. **建国后文献**：1950-1957 阶段大量空白
5. **国际共运专栏**：`international-column/` 仅 1 篇完成（铁流与先锋），2 篇建设中
6. **正名文章**：当前 9 篇，可继续扩充
7. **纪念扩容**：分为三类：现当代小人物、全球工农兵斗争之路、为无产阶级革命事业奋斗的建设者
8. **导师扩容**：扩增人物传记和人物关系网

### 4.2 功能开发

1. **理论拼图优化**：非线性时间轴（密集段放大、稀疏段压缩）、补全 7 篇已制作 HTML 但未加入 puzzle 的文章、扩容理论拼图：用名词（如党、先锋队、资本）串联马列毛主义发展链条
2. **下载中心**：`downloads/` 目录已有 manifest 结构，需前端页面实现
3. **实验性工具**：`experimental.html` 仍为占位符
4. **留言板**：`message-board.html` 有 UI 但无后端
5. **全文搜索**：从客户端索引升级到服务端全文检索（需后端）
6. **阅读进度追踪**：用 localStorage 记录每篇文章滚动位置


### 4.3 技术优化

1. **CSS 清理**：`style.css` 1450+ 行，部分样式可能冗余（如 `.article-header` 同时在 style.css 和 articles-detail.css 定义）
2. **JS 模块化**：`search.js` 600+ 行索引数据散落，可抽离到 data/ 目录
3. **图片懒加载**：860+ 张海报全量加载影响首屏性能
4. **构建流程**：现有 `.py` 脚本服务于一次性批量操作，可整合作业流
5. **无障碍**：aria-label 仅汉堡按钮有，其余交互元素需补充

---

## 五、待开发占位项

### 5.1 页面级占位

| 页面 | 当前状态 | 需要做什么 |
|------|----------|------------|
| `message-board.html` | 空壳 UI，无后端 | 接入留言存储（GitHub Issues / 第三方服务 / 自建后端） |
| `experimental.html` | 占位符 | 设计实验工具：概念关系图生成器？阶级分析计算器？ |
| `international-current.html` | 内容极少 | 时事分析栏目，需确定内容来源与更新机制 |
| `international-memorial.html` | 内容极少 | 纪念板栏目 |
| `gallery/photos.html` | 框架存在 | 待填充照片内容 |
| `gallery/poetry.html` | 框架存在 | 诗词模块已上线但内容待丰富 |

### 5.2 文章级占位（wip）

在 `collections.json` 中标记为 `"status": "wip"` 的文章约 40+ 篇，需逐一：
1. 获取/整理原文 txt → `data/` 目录
2. 生成 HTML（含章节折叠、Tab 切换、图表等交互组件）
3. 加入 puzzle.html 节点
4. 更新合集状态为 ready

### 5.3 功能占位

- **理论拼图非线性轴**：`articles_gap_analysis.txt` 已规划分段公式，待文章数量达 60+ 后实施
- **下载中心**：`downloads/manifest.json` 已定义资源清单，缺少前端下载页面
- **PDF 生成**：`generate_pdfs.py` 存在但需确认可用性
- **文章内图表**：已引入 Chart.js CDN，`_template.html` 有 `<script>` 引用，但大部分文章未使用
- **标签系统**：文章有 keywords 但仅用于搜索索引，无标签云/标签页

---

## 六、维护注意事项

### 6.1 新文章添加入口

添加一篇新文章的标准流程：
1. 在 `articles/` 下基于 `_template.html` 创建 HTML
   - **注意**：模板仍使用 `article-container`，应改为 `article-detail`（见 P0 待办）
2. 在 `<style>` 中覆盖 `:root` 色值
3. 在 `js/search.js` 的 `articleIndex` 中添加条目
4. 如需加入合集，在 `js/main.js` 的 `COLLECTIONS` 变量中添加
5. 如需加入拼图，在 `puzzle.html` 中添加节点
6. 如有章节结构，在 `data/articles_structure.json` 中添加

### 6.2 新合集创建

1. 在 `data/collections.json` 中添加合集定义
2. 在 `js/main.js` 的 `COLLECTIONS` 变量中同步添加（**注意**：目前合集数据在**两个地方**维护）
3. 合集封面 emoji 需在三个位置一致

### 6.3 已知技术债务

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 合集数据三写（collections.json + main.js + articles.html 内联） | 修改需同步三处 | 中 |
| 图片全量加载无懒加载 | 影响首屏性能 | 中 |
| search.js 索引与 HTML 页面数据不同源 | 搜索可能漏掉新文章 | 中 |
| 导航栏在 72 个文件中各自内联 | 修改需逐一更新（当前通过脚本批量处理） | 低 |

### 6.4 部署信息

- 平台：Github（静态托管）
- 配置文件：`vercel.json`（含 txt/pdf 下载 header 设置）
- Git 远程：`git-push.bat` 推送，Github Page 自动部署
- 注意事项：总文件大小需控制 < 100MB（免费额度），图片资源约占主要部分

---

## 七、子文档索引

- [[工作日志]] — 2026-06-19/20 的 CSS 重建与专栏上线记录
- `DEPLOY.md` — 部署指南（GitHub + Vercel）
- `articles_gap_analysis.txt` — 理论拼图时间轴稀疏区分析与填充规划
- `data/collections.json` — 合集定义
- `data/articles_structure.json` — 文章章节结构

---

## 八、文章制作规范

### 8.1 模板文件

`articles/_template-v2.html` — 新建文章时复制此文件，按下方步骤填入内容。

### 8.2 六大分类色值

| 分类 | data-category | --cat-primary | --cat-secondary | 适用 |
|------|-------------|---------------|-----------------|------|
| 哲学基础 | `philosophy` | `#8b0000` | `#c41e3a` | 实践论/矛盾论/反杜林论/唯批等 |
| 政治经济学 | `economics` | `#e65100` | `#ff8f00` | 资本论/帝国主义论/哥达纲领批判等 |
| 政治理论 | `politics` | `#805ad5` | `#9f7aea` | 国家与革命/新民主主义论/共产党宣言等 |
| 党的建设 | `party` | `#38a169` | `#68d391` | 怎么办/论列宁主义基础/学习和时局等 |
| 军事战略 | `military` | `#3182ce` | `#63b3ed` | 论持久战/中国革命战争的战略问题等 |
| 思想文化 | `culture` | `#e6b800` | `#ffd700` | 延安文艺座谈/马克思主义三个来源等 |

每篇文章在 `:root` 中选择对应色值，可在 ± 色域内微调。

### 8.3 制作步骤

1. **复制模板**：`cp _template-v2.html Mao/新文章slug.html`
2. **填 `:root` 色值**：选择分类对应颜色
3. **填 `<body data-category="">`**：填入分类标识
4. **填头**部：星级/标题/副标题/作者/日期/分类/字数/篇幅标签
5. **填原文**：`<div class="chapter">` 块，每章一个，带 `toggleChapter` 点击折叠
6. **填读法提示**：`<div class="guide-block">` 若干
7. **填难点解析**：`<div class="diff-item">` 若干，内含 `.wrong` / `.correct` 对照
8. **填对话空间**：`<div class="dialogue-block">` 若干
9. **填行动实验**：`<div class="action-item">` 若干
10. **填一目了然**：
    - 必选：雷达图（6 维）+ 论证节奏曲线 + 结构分析
    - 可选：额外 `<canvas>` + `ARTICLE_CHARTS` 配置
11. **填理论拼图**：体系定位 / 联结列表 / 时间轴 / conn-card 联结卡片网格（替代原延伸阅读）
12. **设下载链接**：修改 `DOWNLOAD_LINKS` 数组

### 8.4 Tab 规范

| Tab | id | 用途 | 内容组件 |
|-----|-----|------|---------|
| 原文 | `original` | 全文阅读 | `.text-intro` + `.chapter`（折叠）|
| 读法提示 | `reading` | 阅读方法论 | `.guide-block` |
| 难点解析 | `difficulty` | 常见误读纠正 | `.diff-item` > `.wrong` / `.correct` |
| 对话空间 | `dialogue` | 批判性追问 | `.dialogue-block` |
| 行动实验 | `action` | 实践练习 | `.action-item` |
| 一目了然 | `visual` | 图表可视化（核心）| `.chart-block` + Canvas + `.structure-analysis` |
| 理论拼图 | `puzzle` | 体系联结（核心）| `.theory-node` + `.timeline-track` + `.conn-grid` |

### 8.5 共享资源

| 文件 | 说明 |
|------|------|
| `css/articles-detail.css` | 全部组件样式，使用 `var(--cat-*)` 色值 |
| `js/article-common.js` | switchTab / toggleChapter / toggleAllChapters / 图表初始化 / 下载FAB |
| `js/main.js` | 导航汉堡菜单 / 合集导航 / 章节侧边栏 |
| `js/darkmode.js` | 黑夜模式切换 |
| `js/cursor.js` | 自定义光标 |

### 8.6 设计思路

**核心理念**：每篇文章不仅是原文阅读器，更是一个"理解加速器"——通过结构化的 Tab 模块，把读者从"被动阅读"引导到"主动分析"。

**原文为地基**：一切辅助内容围绕原文展开，不替代原文。章节折叠设计让长文可管理，阅读提示帮读者建立预期。

**七 Tab 分工**：

| Tab | 认知层次 | 设计意图 |
|-----|---------|---------|
| 原文 | 接收 | 提供完整文本，章节折叠降低长文压力 |
| 读法提示 | 方法 | 教读者"怎么读"，提供阅读策略和关注重点 |
| 难点解析 | 纠偏 | 预判常见误读，用 wrong/correct 对照直接纠正 |
| 对话空间 | 追问 | 把文章从"独白"变成"对话"，引导读者批判性思考 |
| 行动实验 | 转化 | 把理论转化为可操作的练习，从"读了"到"会了" |
| 一目了然 | 可视化 | 用图表把抽象论证变成直观形状，降低认知门槛 |
| 理论拼图 | 联结 | 把单篇文章放入马哲体系网络，展示思想流变和影响链 |

**一目了然的图表哲学**：
- 雷达图 = "这篇文章长什么样"（6 维性格画像）
- 论证曲线 = "文章的呼吸节奏"（强度起伏，高峰与低谷）
- 结构分析 = "为什么这一段重、那一段轻"（文字解释曲线）
- 特色图表 = "这篇文章独有的思想工具"（按主题自由发挥）

**颜色系统**：六大分类各有古典严肃的基准色。每篇文章在基准色 ± 范围内微调，保持分类辨识度的同时允许个性。

### 8.7 技术约束（关键——违反即 Bug）

#### 8.7.1 图表初始化契约

图表通过 **`ARTICLE_CHARTS` 数组** 声明，由 `article-common.js` 的 `initArticleCharts()` 统一初始化：

```
声明时机：页面加载时定义 var ARTICLE_CHARTS = [...]
初始化时机：用户切换到「一目了然」Tab 时（双 RAF 延迟确保 canvas 可见且有尺寸）
```

**绝对禁止**：在 `<script>` 中直接调用 `new Chart(ctx, {...})` 创建图表。原因：
- canvas 在隐藏的 `tab-content`（`display:none`）中时，Chart.js 无法获取正确尺寸
- 必须等用户点击「一目了然」Tab、canvas 可见后，由 `article-common.js` 统一初始化

**正确模式**（与模板一致）：

```javascript
var ARTICLE_CHARTS = [
    {
        id: 'radarChart',       // ← 必须与 <canvas id="..."> 完全一致
        config: { type: 'radar', data: {...}, options: {...} }
    },
    {
        id: 'argumentCurve',    // ← 必须与 <canvas id="..."> 完全一致
        config: { type: 'line', data: {...}, options: {...} }
    }
    // 可继续添加自定义图表
];
```

#### 8.7.2 必选图表（每篇文章必须包含）

| 序号 | canvas id | 图表类型 | 说明 |
|------|-----------|---------|------|
| 1 | `radarChart` | 雷达图 | 6 维文章特征（理论深度/现实针对性/时间跨度/行动指导性/情感强度/结构复杂度）|
| 2 | `argumentCurve` | 折线图 | 论证节奏曲线，labels 对应章节名，data 对应强度值 0-10 |
| 3 | （文本） | `.structure-analysis` | 结构分析文本，每个 `.analysis-item` 对应曲线的每个数据点 |

可选图表：额外 `<canvas>` + 对应的 `ARTICLE_CHARTS` 条目，数量不限。

#### 8.7.3 图表 ID 匹配规则（硬约束）

`ARTICLE_CHARTS[i].id` **必须** 与 HTML 中 `<canvas id="...">` **逐字符一致**。

常见错误：
- canvas id=`rhythmChart`，config id=`argumentCurve` → 图表不渲染
- canvas 存在但 `ARTICLE_CHARTS` 中无对应条目 → 图表不渲染
- `ARTICLE_CHARTS` 有条目但 canvas 不存在 → 静默跳过

#### 8.7.4 JS 脚本加载顺序（固定）

```html
<script src="../../../js/article-common.js"></script>   <!-- 1. 通用函数 + 图表初始化器 -->
<script>
    var ARTICLE_CHARTS = [...];   <!-- 2. 本文图表声明（必须在 common.js 之后） -->
    var DOWNLOAD_LINKS = [...];   <!-- 3. 下载链接 -->
</script>
<script src="../../../js/main.js"></script>            <!-- 4. 导航/合集/章节 -->
<script src="../../../js/darkmode.js"></script>          <!-- 5. 黑夜模式 -->
<script src="../../../js/cursor.js"></script>            <!-- 6. 自定义光标 -->
```

#### 8.7.5 CSS 约束

- **必须引用** `articles-detail.css`（含所有 Tab/图表/拼图组件的样式和黑夜模式）
- **必须定义** `:root { --cat-primary; --cat-secondary; --cat-gradient; }`（三行色值）
- **禁止** 在 `<style>` 中重复定义 `articles-detail.css` 已有的样式（如 `.tab-nav`/`.chapter`/`.chart-block` 等）
- **允许** 文章特有样式（如特殊标注颜色、正文注释块），放在 `:root` 色值之后

#### 8.7.6 HTML 结构约束

- 文章容器 class 必须为 `article-detail`（非 `article-container`）
- 必须包含全部 7 个 Tab：原文/读法提示/难点解析/对话空间/行动实验/一目了然/理论拼图
- `<body>` 必须含 `data-category` 属性（六选一）
- 下载 FAB 按钮 HTML 固定不变（`article-common.js` 依赖 `#downloadFab` / `#downloadMenu` / `#downloadLinks`）

#### 8.7.7 常见问题速查

| 症状 | 根因 | 解决 |
|------|------|------|
| 切换到一目了然，图表空白 | `ARTICLE_CHARTS[i].id` 与 canvas id 不匹配 | 逐字对齐两个 id |
| 图表只显示在图例区域 | canvas 在隐藏 div 中时被直接 `new Chart()` | 删除所有内联 `new Chart()`，改用 `ARTICLE_CHARTS` 数组 |
| 图表缩放异常/极小 | 同上，canvas 初始化时尺寸为 0 | 同上 |
| 某篇文章缺少论证曲线 | 只有雷达图 canvas，缺 `argumentCurve` canvas 和对应 config | 补全必选图表 |
| 黑夜模式下图表颜色刺眼 | `ARTICLE_CHARTS` 中颜色硬编码 | 使用 `var(--cat-*)` CSS 变量或深色兼容色值 |
| 所有图表静默不渲染（JS 无报错） | `ARTICLE_CHARTS` 数组用单行压缩格式——深层嵌套括号在压缩时可能产生语法歧义，尤其是含 `\n` 转义符的长 label 与深层 `{}` 耦合时 | 展开为多行缩进格式（每层 `{}` 独立成行），参照 `_template-v2.html` 或已修复文件 |

#### 8.7.8 ARTICLE_CHARTS 格式规范（硬约束）

**必须使用多行缩进格式**，禁止单行压缩。原因：经多次实测，单行压缩的深层嵌套对象（三层以上 `{}`）在包含 `\n` 转义符、Unicode 字符（★、→）时，浏览器 JS 解析器产生间歇性静默失败——不抛异常、但数组解析为 `undefined`，导致全部图表静默。

**正确格式**（参照 `_template-v2.html` L322-368）：

```javascript
var ARTICLE_CHARTS = [
    {
        id: 'radarChart',
        config: {
            type: 'radar',
            data: {
                labels: [...],
                datasets: [{
                    label: '...',
                    data: [...],
                    borderColor: '...',
                    backgroundColor: '...',
                    pointBackgroundColor: '...'
                }]
            },
            options: {
                responsive: true,
                scales: { r: { min: 0, max: 10, ticks: { stepSize: 2 } } },
                plugins: { legend: { display: false } }
            }
        }
    },
    // ... 更多图表
];
```

**错误格式**（曾导致 4 篇文章图表静默）：

```javascript
var ARTICLE_CHARTS = [
    { id: 'radarChart', config: { type: 'radar', data: { labels: [...], datasets: [{...}] }, options: {...} } },
    // ↑ 单行压缩：深层嵌套 + 转义符 = 间歇性解析失败
];
```

**历史教训**：论持久战、人民内部矛盾、学习和时局、游击战争、战略问题、战争和战略问题、新民主主义论——7 篇连续出现同症状。根因一致：单行压缩或缺失 Tab 内容。展开 + 补全后全部修复。

**关键认识**：紧凑格式不是在省时间——它在制造 bug。每篇必须独立逐 Tab 检查，所有 ARTICLE_CHARTS 使用多行缩进。

---

## 九、变更记录

### 2026-07-23 全站架构重构

- 所有 HTML 移入 `html/`，文章按导师分入 `articles/Mao/` `Marx/` 等子文件夹
- 首页新增开屏动画 + 百叶窗轮播（8 张卡片，5s/张）
- 自定义光标（红五星/镰刀锤头）+ 光辉效果
- 共享 `darkmode.js` / `cursor.js`
- 修复：4 篇 `article-container`→`article-detail`；36 个文件残留 `}` 清除；5 个导师页 Chart.js 双帧 RAF 修复；合集 slugs 更新

### 2026-07-25 全站 29 篇经典文章规范化

按 `_template-v2.html` + `articles-detail.css` + `article-common.js` 基准，逐篇重写 Engels（2）、Marx（6）、Lenin（6）、Mao（10 非基准）、Stalin（2，此前已完成）。每篇统一执行：

- `data-category` 归位 `<body>`（英文值）；8 tab→7（延伸阅读并入 puzzle-modern）
- 内联 `new Chart()`→`ARTICLE_CHARTS` 多行数组；`switchTab`/`toggleChapter`→`article-common.js`
- 内联下载 JS→`DOWNLOAD_LINKS` 数组 + 统一 FAB
- 重复 CSS 全删（每篇仅保留 `:root` 三行）；旧类名统一（`.reading-tip`→`.guide-block` 等）；h2 添加 `tab-heading` 类
- 色值从不规范自定义（海军蓝/绿色）统一为六大分类标准色
- 必选图表（radarChart + argumentCurve + 结构分析）全部补全
- 维护文档第八节补充完整技术规范（`8.7.1` ~ `8.7.8`），包括 ARTICLE_CHARTS 多行格式硬约束及历史案例
